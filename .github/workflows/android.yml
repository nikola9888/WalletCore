name: Build WalletCore Android

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build APK
        uses: ArtemMihalko/buildozer-action@v1
        with:
          command: buildozer android debug
